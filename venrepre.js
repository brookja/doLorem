// Assuming you want to rotate an element with the ID "hour" based on the value of the variable "hour":
const hour = 90; // Replace with the actual value you want

// Set the CSS transform property to rotate the element
$('#hour').css("transform", `rotate(${hour}deg)`);
